import React from "react"

import LoginPage from "components/ui/login"

export default {
  title: "UI/Login",
  component: LoginPage
}

const Template: React.VFC = () => <LoginPage/>

export const Login = Template.bind({})